import RoundAnchorView from './RoundAnchorView';

const RoundAnchor = ({left, top}: {left: number, top: number}) => {
    return (
        <RoundAnchorView left={left} top={top}/>
    );
};

export default RoundAnchor;